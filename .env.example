# s3loadtest Configuration
# Copy this file to .env and fill in your values.
# Environment variables take priority over .env values.

# ---------------------------------------------------------------------------
# S3 Connection (REQUIRED)
# ---------------------------------------------------------------------------

# S3 endpoint URLs (comma-separated for multi-endpoint rotation)
S3_ENDPOINTS=https://your-s3-endpoint:9000

# S3 credentials
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key

# S3 bucket name
S3_BUCKET=your-bucket-name

# AWS region (default: us-east-1)
# AWS_REGION=us-east-1

# ---------------------------------------------------------------------------
# S3 Client Backend
# ---------------------------------------------------------------------------

# Backend selection: boto3 (default), proxy (s3pool), minio
# S3LOADTEST_BACKEND=boto3

# SSL verification: true or false (default: false for self-signed certs)
# S3_VERIFY_SSL=false

# ---------------------------------------------------------------------------
# Paths (all optional — sensible defaults used if not set)
# ---------------------------------------------------------------------------

# Pre-generated test data files (default: ./data)
# S3LOADTEST_DATA_DIR=./data

# Log file output (default: ./logs)
# S3LOADTEST_LOG_DIR=./logs

# Workers file — list of hostnames for distributed mode (default: ./workers)
# S3LOADTEST_WORKERS_FILE=./workers

# Temp directory for key tracking files (default: /tmp)
# S3LOADTEST_TEMP_DIR=/tmp

# Key tracking file directory (default: same as TEMP_DIR)
# S3LOADTEST_KEY_DIR=/tmp

# ---------------------------------------------------------------------------
# Distributed Mode (optional — for SSH-based multi-machine tests)
# ---------------------------------------------------------------------------

# Code directory on remote workers
# S3LOADTEST_REMOTE_CODE_DIR=/path/to/s3loadtest/on/workers

# Log directory on remote workers
# S3LOADTEST_REMOTE_LOG_DIR=/path/to/logs/on/workers

# ---------------------------------------------------------------------------
# s3pool Proxy (optional — only needed when S3LOADTEST_BACKEND=proxy)
# ---------------------------------------------------------------------------

# Path to s3pool binary (default: found on PATH)
# S3POOL_BINARY=s3pool

# Proxy listen address (default: 0.0.0.0:18080)
# S3LOADTEST_PROXY_ADDR=0.0.0.0:18080

# Directory containing .env for s3pool proxy
# S3LOADTEST_ENV_DIR=.
